# 关于“创建数据库” (Create Database) 功能的技术确认

哈基米，我已收到您的新需求：**在最外层列表下创建一个包含所有子项的数据库**。

在开始开发 `src/features/data` 之前，我需要确认几个关键技术细节，因为思源的标准 API 文档中并没有直接提供“创建并配置属性视图 (Attribute View / Database)”的接口：

1.  **数据库 (Database) 的具体形式**：
    *   您是指创建一个 **属性视图 (Attribute View, block type="av")** 吗？
    *   如果是，目前思源是否有公开 API 可以直接设置 AV 的视图配置（例如：筛选条件、显示的列）？
    *   还是说我们通过 `insertBlock` 插入一个 `av` 类型的块，然后通过某种方式（如 IAL 属性）来绑定查询条件？

2.  **数据的填充方式**：
    *   您希望数据库如何“包含”子项？
    *   **方案 A (查询式)**: 数据库视图自动通过 SQL 或 ID 列表查询并显示这些列表项（列表项本身位置不变）。
    *   **方案 B (移动式)**: 将列表项**移动**到数据库块内部（如果 AV 支持这种结构）。
    *   **方案 C (关联式)**: 创建一个新的数据库，并在其中创建指向列表项的关联或引用。

3.  **“所有子项”的定义**：
    *   是指列表的**直接子节点**？
    *   还是**递归**包含所有层级的子节点？

4.  **推荐实现路径**：
    *   如果 API 限制较多，我们可以尝试通过 `insertBlock` 插入一段包含 Attribute View DOM 结构的 HTML。您是否有该结构的示例或相关 API 提示？

请在回复中提供指导，我将根据您的指示进行开发！
